---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "mtncloud_budget Resource - mtncloud"
subcategory: ""
description: |-
  Manages an MTN Cloud cost budget over a yearly period at a chosen interval.
---

# mtncloud_budget (Resource)

Manages an MTN Cloud cost budget over a yearly period at a chosen interval.

## Example Usage

```terraform
# A monthly budget for 2026 — costs must have 12 entries (one per month).
resource "mtncloud_budget" "monthly" {
  name     = "platform-2026"
  scope    = "account"
  interval = "month"
  year     = "2026"
  currency = "NGN"
  costs    = [1000, 1000, 1000, 1000, 1000, 1000, 1000, 1000, 1000, 1000, 1000, 1000]
}

# A single annual budget — costs has 1 entry.
resource "mtncloud_budget" "annual" {
  name     = "annual-cap-2026"
  interval = "year"
  year     = "2026"
  costs    = [12000]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `costs` (List of Number) Budget amounts per interval. Length must match `interval` (1/4/12).
- `name` (String) Name of the budget.

### Optional

- `description` (String) Description of the budget.
- `enabled` (Boolean) Whether the budget is enabled. Defaults to `true`.
- `interval` (String) Budget interval. One of `year` (1 cost), `quarter` (4 costs), `month` (12 costs). The length of `costs` must match. Defaults to `year`.
- `rollover` (Boolean) Whether unused budget rolls over between intervals. Defaults to `false`.
- `scope` (String) Scope the budget applies to (e.g. `account`, `user`, `group`, `cloud`). Defaults to `account`.
- `timezone` (String) Timezone for the budget period. Defaults to `UTC`.
- `year` (String) Calendar year the budget applies to, e.g. `2026`.

### Read-Only

- `currency` (String) Currency code for the budget amounts. Set by MTN Cloud to the account currency (the budget API ignores any requested currency), so this is read-only.
- `id` (String) Numeric identifier of the budget.
